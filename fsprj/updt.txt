



def updatec(i):

    def update_newlist(j):
        with open('data.csv','w',newline='') as file:
            writer = csv.writer(file)
            writer.writerow(j)
    new_list = []
    
    global CropName, SeedVariety, FertiliserUsed, QuantityAvail, PricePkg
    
    SeedVariety = i[0]
    CropName = i[1]
    SeedVariety = i[2]
    FertiliserUsed = i[3]
    QuantityAvail = i[4]
    PricePkg = i[5]
    with open('data.csv','r') as file:
        reader = csv.reader(file)
        for row in reader:
            new_list.append(row)
            for element in row:
                data = [CropName, SeedVariety, FertiliserUsed, QuantityAvail, PricePkg]
                index = new_list.index(row)
                new_list[index] = data
    
    update_newlist(new_list)